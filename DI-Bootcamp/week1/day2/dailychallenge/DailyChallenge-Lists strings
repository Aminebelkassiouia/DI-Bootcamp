# Challenge 1: Multiples of a Number
nombre=int(input("Entrey un nombre : "))
longueur=int(input("Entrey la longueur de la liste : "))
multiples=[]
for i in range(1, longueur+1):
    multiples.append(nombre*i)
print(multiples)  
# Challenge 2: Remove Consecutive Duplicate Letters 
texte= input("Entrez une chaine : ")
resultat=""
for i in range(len(texte)):
    if i==0 or texte[i] != texte[i-1]:
        resultat+=texte[i]
print(resultat)        